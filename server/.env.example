
# Server
PORT=5000

# Firebase - Replace these with your Firebase project details
FIREBASE_API_KEY=your-api-key-here  # REPLACE: Get this from Firebase console > Project settings > General
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com  # REPLACE: Format: your-project-id.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id-here  # REPLACE: Find in Firebase console > Project settings
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com  # REPLACE: Format: your-project-id.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id-here  # REPLACE: Find in Firebase console > Project settings
FIREBASE_APP_ID=your-app-id-here  # REPLACE: Find in Firebase console > Project settings
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com  # REPLACE: From your Firebase service account JSON file
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour Private Key\n-----END PRIVATE KEY-----\n"  # REPLACE: From your Firebase service account JSON file
FIREBASE_DATABASE_URL=https://your-project-id.firebaseio.com  # REPLACE: May not be needed if not using Realtime Database

# Paystack - Get this from your Paystack dashboard
PAYSTACK_SECRET_KEY=your-paystack-secret-key-here  # REPLACE: Find in Paystack dashboard > Settings > API Keys & Webhooks

# IMPORTANT: After updating this template, rename this file to .env (without .example)
# NEVER commit your actual .env file with real credentials to version control
